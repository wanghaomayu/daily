import React, {Component} from 'react';

function addNameProp(){Com}{
  return class extends Component{
    render(){
      const {name, ...props} = this.props;
      return (
        
      )
    }
  }
}
